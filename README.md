# NN-Fraud-Detection-Diagnostic-Model
Credit card fraud detection using a neural network, with in-depth model selection and evaluation: bias/variance, class imbalance, threshold tuning, and regularization.
